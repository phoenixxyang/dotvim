Installation:

    git clone http://github.com/phoenixxyang/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/_vimrc ~/.vimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
